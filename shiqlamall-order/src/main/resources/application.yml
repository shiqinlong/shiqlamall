spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 122324
    url: jdbc:mysql://localhost:3306/shiqlamall_oms
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  application:
    name: shiqlamall-order

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 9000
